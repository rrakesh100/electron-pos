{
  "name": "electron-pos",
  "productName": "Electron POS with sqlite",
  "description": "Starter for electron + sqlite POS application",
  "version": "1.0.0",
  "main": "app/app.js",
  "build": {
    "appId": "com.mrp.pos",
    "files": [
      "app/**/*",
      "node_modules/**/*",
      "package.json"
    ]
  },
  "scripts": {
    "postinstall": "install-app-deps",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "build"
  },
  "dependencies": {
    "sqlite3": "^3.1.8"
  },
  "devDependencies": {
    "electron": "^1.4.10",
    "electron-builder": "^10.8.1"
  }
}
